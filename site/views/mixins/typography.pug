mixin typography(options)
  - 
    const { variant = 'body',  text, class: extraClass = '', color='default' } = options;
    const classList = [...( extraClass ).split(' '), 'typography', `typography--${variant}`, `typography--${color}`]
  p(class=classList.join(' ')) #{text} 
