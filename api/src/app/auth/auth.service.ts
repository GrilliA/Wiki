import logger from "../../helper/logger";
import db from "../../database";

export const sendForgottenPasswordEmail = async () => {
  try {
    console.error("to implement");
  } catch (error) {
    logger.error(error);
  }
};

export const seedDatabase = async () => {
  console.error("seeding db to be implemented");
};
